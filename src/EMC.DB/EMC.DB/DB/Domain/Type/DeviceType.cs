using EPFramework.DB;
using System.ComponentModel.DataAnnotations;

namespace EMC.DB
{
    public enum DeviceType
    {
        MotionController,
        IODevice,
        Camera,
        Laser,
    }
}
