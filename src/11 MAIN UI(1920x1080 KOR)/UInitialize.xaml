<Window x:Class="EGGPLANT.UInitialize"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="True" Height="260" Width="560" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <!-- 버전: 우상단 -->
            <TextBlock 
                Foreground="White"
                Text="v 25.08"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="5" />

            <!-- 중앙 제목 -->
            <TextBlock 
                Foreground="White"
                Text="프로그램 구동 중..."
                Margin="20"
                FontSize="18"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3.5*"/>
                <ColumnDefinition Width="6.5*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" HorizontalAlignment="Left" Margin="5">
                <Label>EggPlant Log</Label>
                <TextBlock Foreground="White" Text="TEL : " />
                <TextBlock Foreground="White" Text="FAX : " />
            </StackPanel>

            <Border Grid.Column="1"  Margin="5">
                <TextBlock Text="명함" />
            </Border>
        </Grid>
        <StackPanel Grid.Row="2">
            <!-- Progress bar -->
            <ProgressBar Height="12" Minimum="0" Maximum="100" Margin="10, 0, 10, 0"
                 Value="{Binding Progress}" BorderThickness="0"
                 Background="White"/>

            <!-- 단계 리스트(간단 로그) -->
            <TextBlock TextAlignment="Center" Foreground="White" Text="{Binding CurrentTitle}"></TextBlock>
        </StackPanel>

    </Grid>
</Window>
