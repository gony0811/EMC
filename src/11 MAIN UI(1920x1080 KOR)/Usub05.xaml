<Page x:Class="EGGPLANT.USub05"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EGGPLANT"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="에러 목록">
    <Page.Resources>
        <ResourceDictionary>
            <Style TargetType="TextBlock" BasedOn="{StaticResource MahApps.Styles.TextBlock.AutoCollapsing}">
                <Setter Property="Foreground" Value="White"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="White"></Setter>
                <Setter Property="Foreground" Value="Black"></Setter>
            </Style>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.65*"/>
            <ColumnDefinition Width="5"/>
            <!-- GridSplitter -->
            <ColumnDefinition Width="0.35*"/>
        </Grid.ColumnDefinitions>

        <!-- 좌측: 에러 리스트 (0.65*) -->
        <DataGrid Grid.Column="0"
              ItemsSource="{Binding ErrorList}"
              SelectedItem="{Binding SelectedError, Mode=TwoWay}"
              AutoGenerateColumns="False"
              CanUserAddRows="True"     
              IsReadOnly="True"
              HeadersVisibility="Column"
              RowHeaderWidth="0"
              FontSize="16"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              EnableRowVirtualization="True"
              ScrollViewer.CanContentScroll="True"
              Margin="0,8,8,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="에러 번호" Binding="{Binding Id}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="에러 내용" Binding="{Binding ErrorContents}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 가운데 스플리터 -->
        <GridSplitter Grid.Column="1"
                Width="5"
                HorizontalAlignment="Stretch"
                Background="#22000000"
                ShowsPreview="True"/>

        <!-- 편집부 -->
        <StackPanel Grid.Column="2" Margin="10" DataContext="{Binding SelectedError}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.1*"></ColumnDefinition>
                    <ColumnDefinition Width="0.1*"></ColumnDefinition>
                    <ColumnDefinition Width="0.8*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <Border Grid.Column="1" Margin="4,0,4,0">
                    <TextBlock Grid.Column="1" FontSize="15" Text="에러명" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </Border>
                <TextBox Grid.Column="2" Text="{Binding ErrorContents, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ></TextBox>
            </Grid>
            <TextBlock Text="원인" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Cause, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Height="120"/>

            <TextBlock Text="해결 방법" FontWeight="SemiBold" Margin="0,12,0,4"/>
            <TextBox Text="{Binding Solution, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Height="160"/>
        </StackPanel>

    </Grid>
</Page>
