<Page x:Class="EMC.USub08"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EMC"
      mc:Ignorable="d" 
      Title="USub08">

    <TabControl>
        <TabItem Header="POWER PMAC">
            <Grid local:GridHelper.Columns="2*, 1*" local:GridHelper.Rows="Auto, 1*">

                <Grid local:GridHelper.Rows="1*, Auto" Grid.ColumnSpan="2" Grid.Row="0">
                    <DataGrid 
                        ItemsSource="{Binding PowerPMacList}"
                        SelectedItem="{Binding SelectedPMac, Mode=TwoWay}"
                        AutoGenerateColumns="False"
                        CanUserAddRows="True"
                        IsReadOnly="True"
                        HeadersVisibility="Column"
                        RowHeaderWidth="0"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling"
                        EnableRowVirtualization="True"
                        ScrollViewer.CanContentScroll="True"
                        PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown"
                        Margin="10">

                        <DataGrid.ContextMenu>
                            <ContextMenu 
                            DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="연결" Command="{Binding ConnectPowerPMacCommand}" />
                                <MenuItem Header="연결해제" Command="{Binding DisconnectCommand}" />
                                <Separator/>
                                <MenuItem Header="수정" Command="{Binding EditCommand}" />
                                <MenuItem Header="삭제" Command="{Binding DeleteCommand}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Number" Binding="{Binding Number}" Width="1*" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1*" />
                            <DataGridTextColumn Header="Ip" Binding="{Binding Ip}" Width="1*" />
                            <DataGridTextColumn Header="상태" Binding="{Binding IsConnected}" Width="1*" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
                        <Button Content="생성" Command="{Binding PowerPMacCreateCommand}" />
                    </StackPanel>
                </Grid>
                <Border Grid.Column="0" Grid.Row="1" Background="Gray">
                    <Grid local:GridHelper.Rows="Auto, 1*, Auto">
                        <TextBlock Text="Motion Parameter" FontSize="20" FontWeight="Bold"/>

                        <DataGrid 
                                Grid.Row="1"
                                ItemsSource="{Binding SelectedPMac.MotionList}"
                                SelectedItem="{Binding SelectedMotion, Mode=TwoWay}"
                                AutoGenerateColumns="False"
                                CanUserAddRows="True"
                                IsReadOnly="True"
                                HeadersVisibility="Column"
                                RowHeaderWidth="0"
                                VirtualizingPanel.IsVirtualizing="True"
                                VirtualizingPanel.VirtualizationMode="Recycling"
                                EnableRowVirtualization="True"
                                ScrollViewer.CanContentScroll="True"
                                PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown"
                                Margin="10">

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="모터 번호" Binding="{Binding MotorNo}" Width="1*" />
                                <DataGridTextColumn Header="모터명" Binding="{Binding Name}" Width="1*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSaveFailed}" Value="True">
                                                    <Setter Property="Foreground" Value="Red"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="제조사" Binding="{Binding Maker}" Width="1*" />
                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Content="Motion 생성" Command="{Binding MotionCreateCommand}" Grid.Row="2" HorizontalAlignment="Right" Width="120" Height="40"/>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Grid.Row="1" Background="DarkGray">
                    <Grid local:GridHelper.Rows="Auto, 1*, Auto">
                        <TextBlock Text="메타 정보" FontSize="20" FontWeight="Bold"/>
                        <TabControl Grid.Row="1" Margin="10" DataContext="{Binding SelectedMotion}">
                            <!-- === 일반 설정 탭 === -->
                            <TabItem Header="일반 설정">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel Margin="10">

                                        <!-- 기본 정보 -->
                                        <Expander Header="기본 정보" IsExpanded="True">
                                            <UniformGrid Columns="2" Rows="2" Margin="5">
                                                <local:KeyValueBox Key="모터 번호" Value="{Binding MotorNo}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="모터명" Value="{Binding Name}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="제조사" Value="{Binding Maker}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="타입" Value="{Binding Type}" Width="280" Height="36" Margin="3"/>
                                            </UniformGrid>
                                        </Expander>

                                        <!-- 제어 설정 -->
                                        <Expander Header="제어 설정" IsExpanded="True">
                                            <UniformGrid Columns="2" Margin="5">
                                                <local:KeyValueBox Key="Emergency Enable" Value="{Binding EmergencyEnable}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="Soft Limit Enable" Value="{Binding SoftLimitEnable}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="InPosition Enable" Value="{Binding InPositionEnable}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="Servo Emergency" Value="{Binding ServoOffOnEmergencyEnable}" Width="280" Height="36" Margin="3"/>
                                            </UniformGrid>
                                        </Expander>

                                        <!-- 신호 레벨 -->
                                        <Expander Header="신호 레벨" IsExpanded="True">
                                            <UniformGrid Columns="2" Margin="5">
                                                <local:KeyValueBox Key="Servo" Value="{Binding ServoLevel}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="Alarm" Value="{Binding AlarmLevel}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="Origin" Value="{Binding OriginLevel}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="Emergency" Value="{Binding EmergencyLevel}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="InPosition" Value="{Binding InPositionLevel}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="+Limit" Value="{Binding PositiveLimitLevel}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="-Limit" Value="{Binding NegativeLimitLevel}" Width="280" Height="36" Margin="3"/>
                                            </UniformGrid>
                                        </Expander>

                                    </StackPanel>
                                </ScrollViewer>
                            </TabItem>

                            <!-- === 모션 모드 탭 === -->
                            <TabItem Header="모션 모드">
                                <StackPanel Margin="10">
                                    <Expander Header="홈 모드" IsExpanded="True">
                                        <UniformGrid Columns="2" Margin="5">
                                            <local:KeyValueBox Key="Home Mode 1" Value="{Binding HomeMode1}" Width="280" Height="36" Margin="3"/>
                                            <local:KeyValueBox Key="Home Mode 2" Value="{Binding HomeMode2}" Width="280" Height="36" Margin="3"/>
                                        </UniformGrid>
                                    </Expander>
                                    <Expander Header="속도 모드" IsExpanded="True">
                                        <UniformGrid Columns="2" Margin="5">
                                            <local:KeyValueBox Key="Speed Mode" Value="{Binding SpeedMode}" Width="280" Height="36" Margin="3"/>
                                        </UniformGrid>
                                    </Expander>
                                    <Expander Header="리밋 모드" IsExpanded="True">
                                        <UniformGrid Columns="2" Margin="5">
                                            <local:KeyValueBox Key="+Limit Mode" Value="{Binding PositiveLimitMode}" Width="280" Height="36" Margin="3"/>
                                            <local:KeyValueBox Key="-Limit Mode" Value="{Binding NegativeLimitMode}" Width="280" Height="36" Margin="3"/>
                                        </UniformGrid>
                                    </Expander>

                                    <Expander Header="엔코더/펄스" IsExpanded="True">
                                        <UniformGrid Columns="2" Margin="5">
                                            <local:KeyValueBox Key="Encoder Reverse" Value="{Binding EncoderReverse}" Width="280" Height="36" Margin="3"/>
                                            <local:KeyValueBox Key="Pulse Output" Value="{Binding PulseOutputMethod}" Width="280" Height="36" Margin="3"/>
                                            <local:KeyValueBox Key="Encoder Input" Value="{Binding EncoderInputMethod}" Width="280" Height="36" Margin="3"/>
                                        </UniformGrid>
                                    </Expander>
                                </StackPanel>
                            </TabItem>

                            <!-- === 동작 파라미터 탭 === -->
                            <TabItem Header="동작 파라미터">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel Margin="10">

                                        <Expander Header="이동 파라미터" IsExpanded="True">
                                            <UniformGrid Columns="2" Margin="5">
                                                <local:KeyValueBox Key="Ratio" Value="{Binding Ratio}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="Max Velocity" Value="{Binding MaxVelocity}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="Position Error" Value="{Binding PositionError}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="InPosition Width" Value="{Binding InPositionWidth}" Width="280" Height="36" Margin="3"/>
                                            </UniformGrid>
                                        </Expander>

                                        <Expander Header="소프트 리밋" IsExpanded="False">
                                            <UniformGrid Columns="2" Margin="5">
                                                <local:KeyValueBox Key="+Soft Limit" Value="{Binding SoftPositiveLimit}" Width="280" Height="36" Margin="3"/>
                                                <local:KeyValueBox Key="-Soft Limit" Value="{Binding SoftNegativeLimit}" Width="280" Height="36" Margin="3"/>
                                            </UniformGrid>
                                        </Expander>

                                        <Expander Header="홈 설정" IsExpanded="True">
                                            <StackPanel Margin="10">

                                                <!-- === Offset === -->
                                                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="4" Margin="0,5,0,5">
                                                    <StackPanel>
                                                        <Border Background="Gray" CornerRadius="4,4,0,0">
                                                            <TextBlock Text="Offset"
                                                                       Margin="5"
                                                                       FontSize="14"
                                                                       FontWeight="Bold"
                                                                       Foreground="White"/>
                                                        </Border>
                                                        <UniformGrid Columns="1" Margin="5">
                                                            <local:KeyValueBox Key="Home Offset"
                                                                               Value="{Binding HomeOffset}"
                                                                               Width="280"
                                                                               Height="36"
                                                                               Margin="3"/>
                                                        </UniformGrid>
                                                    </StackPanel>
                                                </Border>

                                                <!-- === 속도 설정 === -->
                                                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="4" Margin="0,5,0,5">
                                                    <StackPanel>
                                                        <Border Background="Gray" CornerRadius="4,4,0,0">
                                                            <TextBlock Text="속도 설정"
                                                                       Margin="5"
                                                                       FontSize="14"
                                                                       FontWeight="Bold"
                                                                       Foreground="White"/>
                                                        </Border>
                                                        <UniformGrid Columns="2" Margin="5">
                                                            <local:KeyValueBox Key="Home V1" Value="{Binding HomeVelocity1}" Width="280" Height="36" Margin="3"/>
                                                            <local:KeyValueBox Key="Home V2" Value="{Binding HomeVelocity2}" Width="280" Height="36" Margin="3"/>
                                                            <local:KeyValueBox Key="Home V3" Value="{Binding HomeVelocity3}" Width="280" Height="36" Margin="3"/>
                                                        </UniformGrid>
                                                    </StackPanel>
                                                </Border>

                                                <!-- === 가속도 설정 === -->
                                                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="4" Margin="0,5,0,5">
                                                    <StackPanel>
                                                        <Border Background="Gray" CornerRadius="4,4,0,0">
                                                            <TextBlock Text="가속도 설정"
                                                                       Margin="5"
                                                                       FontSize="14"
                                                                       FontWeight="Bold"
                                                                       Foreground="White"/>
                                                        </Border>
                                                        <UniformGrid Columns="2" Margin="5">
                                                            <local:KeyValueBox Key="Home A1" Value="{Binding HomeAccelerate1}" Width="280" Height="36" Margin="3"/>
                                                            <local:KeyValueBox Key="Home A2" Value="{Binding HomeAccelerate2}" Width="280" Height="36" Margin="3"/>
                                                        </UniformGrid>
                                                    </StackPanel>
                                                </Border>

                                                <!-- === 감속 설정 === -->
                                                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="4" Margin="0,5,0,5">
                                                    <StackPanel>
                                                        <Border Background="Gray" CornerRadius="4,4,0,0">
                                                            <TextBlock Text="감속 설정"
                                                                       Margin="5"
                                                                       FontSize="14"
                                                                       FontWeight="Bold"
                                                                       Foreground="White"/>
                                                        </Border>
                                                        <UniformGrid Columns="2" Margin="5">
                                                            <local:KeyValueBox Key="Home D1" Value="{Binding HomeDecelerate1}" Width="280" Height="36" Margin="3"/>
                                                            <local:KeyValueBox Key="Home D2" Value="{Binding HomeDecelerate2}" Width="280" Height="36" Margin="3"/>
                                                        </UniformGrid>
                                                    </StackPanel>
                                                </Border>

                                            </StackPanel>

                                        </Expander>

                                    </StackPanel>
                                </ScrollViewer>
                            </TabItem>
                        </TabControl>
                        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
                            <Button Content="저장" Command="{Binding MotionSaveCommand}" Width="80"  Height="40"/>
                        </StackPanel>
                    </Grid>
            </Border>
            </Grid>
            
        </TabItem>
    </TabControl>
</Page>
