<Page x:Class="EMC.USub06"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:EMC"
      xmlns:uc="clr-namespace:EMC"
      xmlns:page="clr-namespace:EMC"
      mc:Ignorable="d"
      Title="USub06">
    <!-- 모터 제어 화면 -->
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/01 SYSTEM/Common/Styles/DefaultResource.xaml"/>
                <ResourceDictionary Source="/01 SYSTEM/Common/Styles/ButtonsAndSwitches.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Border Style="{StaticResource Panel.Radius5}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Border Grid.Column="0" Grid.Row="0" Margin="10">
                <uc:TitleBar Title="모터 제어"/>
            </Border>

            <GroupBox Header="모션 항목"
                      Style="{StaticResource TitledBox}"
                      Grid.Column="0" Grid.Row="1" Grid.RowSpan="2"
                      Margin="10" Width="250">
                <ListBox
                    ItemsSource="{Binding PowerPmacMotionList}"
                    SelectedItem="{Binding SelectedMotion, Mode=TwoWay}"
                    Background="{x:Null}"
                    BorderThickness="0"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionMode="Single">

                    <!-- 컨테이너(아이템) 자체는 Stretch + 투명 배경으로 Hover 히트 누락 없게 -->
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="4"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="SnapsToDevicePixels" Value="True"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <!-- 버튼처럼 보이는 템플릿 -->
                    <ListBox.ItemTemplate>
                        <DataTemplate >
                            <ToggleButton              
                                    Height="40"
                                    FocusVisualStyle="{x:Null}"
                                    IsChecked="{Binding
                                        RelativeSource={RelativeSource AncestorType=ListBoxItem},
                                        Path=IsSelected,
                                        Mode=TwoWay,
                                        UpdateSourceTrigger=PropertyChanged}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"/>
                                </StackPanel>
                            </ToggleButton>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
            <Border Grid.Column="1" Grid.Row="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.7*"></ColumnDefinition>
                        <ColumnDefinition Width="0.3*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <GroupBox Header="위치" 
                              Grid.Column="0" 
                              Style="{StaticResource TitledBox}" 
                              Margin="0,10,0,10">
                        <Grid local:GridHelper.Rows="1*, Auto">

                            <DataGrid
                                x:Name="PositionDataGrid"
                                Style="{StaticResource DataGrid.Transparent}"
                                ItemsSource="{Binding SelectedMotion.PositionList}"
                                SelectedItem="{Binding SelectedPosition, Mode=TwoWay}"
                                FontSize="16"
                                AutoGenerateColumns="False"
                                IsReadOnly="True"
                                CanUserAddRows="False"
                                CanUserDeleteRows="False"
                                HeadersVisibility="Column"
                                SelectionMode="Single"
                                EnableRowVirtualization="True"
                                EnableColumnVirtualization="True"
                                RowHeaderWidth="0"
                                GridLinesVisibility="None">

                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <!-- 우클릭 시 행 선택(이미 쓰시는 핸들러 유지) -->
                                        <EventSetter Event="MouseRightButtonDown" Handler="DataGridRow_MouseRightButtonDown"/>

                                        <!-- ✅ Row.Tag = DataGrid의 ViewModel -->
                                        <Setter Property="Tag"
                    Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>

                                        <Setter Property="ContextMenu">
                                            <Setter.Value>
                                                <ContextMenu>
                                                    <!-- 수정 -->
                                                    <MenuItem Header="수정"
                                  Command="{Binding PlacementTarget.Tag.PositionUpdateCommand,
                                                    RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                  CommandParameter="{Binding PlacementTarget.DataContext,
                                                             RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                    <!-- 삭제 -->
                                                    <MenuItem Header="삭제"
                                  Command="{Binding PlacementTarget.Tag.PositionDeleteCommand,
                                                    RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                  CommandParameter="{Binding PlacementTarget.DataContext,
                                                             RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                </ContextMenu>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </DataGrid.RowStyle>


                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="이름"
                                        Binding="{Binding Name}"
                                        Width="*"
                                        SortMemberPath="Name"/>
                                    <DataGridTextColumn Header="위치"
                                        Binding="{Binding Location, StringFormat=F3}"
                                        Width="140"
                                        SortMemberPath="Location"/>
                                    <DataGridTextColumn Header="속도"
                                        Binding="{Binding Speed, StringFormat=F2}"
                                        Width="120"
                                        SortMemberPath="Speed"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <Button Content="생성" Command="{Binding PositionCreateCommand}" Grid.Row="1"></Button>
                        </Grid>

                    </GroupBox>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0.4*"></RowDefinition>
                            <RowDefinition Height="0.6*"></RowDefinition>
                        </Grid.RowDefinitions>


                        <!-- 모터 상태 관리 -->
                        <GroupBox Style="{StaticResource TitledBox}" Header="모터 상태 관리" Grid.Row="0" Margin="10">
                            <uc:MotorStateControl x:Name="MotorState"></uc:MotorStateControl>
                        </GroupBox>

                        <!-- 모터 조작 -->
                        <GroupBox Style="{StaticResource TitledBox}" Header="조작" Grid.Row="1" Margin="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.7*"></ColumnDefinition>
                                    <ColumnDefinition Width="0.3*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal" Margin="10">
                                        <TextBox  Style="{StaticResource TextBox.Rounded.Panel}" Width="150" Margin="5,0,0,0"></TextBox>
                                        <TextBlock Text="um" Margin="5,0,0,0"></TextBlock>
                                    </StackPanel>

                                    <!-- 조작 버튼 -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="0.3*"></ColumnDefinition>
                                            <ColumnDefinition Width="0.4*"></ColumnDefinition>
                                            <ColumnDefinition Width="0.3*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Button Content="-1um" Grid.Column="0" Grid.Row="0" Margin="2" MinHeight="40"/>

                                        <Button Content="-0.1um" Grid.Column="0" Grid.Row="1" Margin="2" />
                                        <Button Content="-0.01um" Grid.Column="0" Grid.Row="2" Margin="2" />
                                        <Button Style="{StaticResource Button.Circle.WithRing}" 
                                                Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" 
                                                Width="100" Height="100"></Button>

                                        <Button Content="1um" Grid.Column="2" Grid.Row="0" Margin="2" MinHeight="40"/>

                                        <Button Content="0.1um" Grid.Column="2" Grid.Row="1" Margin="2" />
                                        <Button Content="0.01um" Grid.Column="2" Grid.Row="2" Margin="2" />
                                        <Button Content="정지" Background="Red" Grid.Column="1" Grid.Row="2" Margin="2"/>
                                    </Grid>
                                </StackPanel>


                                <Grid Grid.Column="1" VerticalAlignment="Top">
                                    <!-- Speed -->
                                    <uc:LabelSlider TitleLabel="속도" 
                                                    Minimum="1" Maximum="100"
                                                    Height="230"
                                                    TickFrequency="1"
                                                    IsSnapToTickEnabled="True"
                                                    Value="{Binding Speed}"/>
                                </Grid>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
    
</Page>
