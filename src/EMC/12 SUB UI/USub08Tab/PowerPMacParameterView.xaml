<Page x:Class="EMC.PowerPMacParameterView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:EMC"
      Title="Power PMAC Parameter">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- 모션 파라미터 목록 -->
        <DataGrid 
            Grid.Row="1"
            ItemsSource="{Binding Motions}"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            Margin="0,10,0,10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="MotorNo" Binding="{Binding MotorNo}" Width="100" />
                <DataGridTextColumn Header="Maker" Binding="{Binding Maker}" Width="200" />
                <DataGridTextColumn Header="MaxVelocity" Binding="{Binding MaxVelocity}" Width="150" />
                <DataGridTextColumn Header="Ratio" Binding="{Binding Ratio}" Width="150" />
                <DataGridTextColumn Header="SoftPositiveLimit" Binding="{Binding SoftPositiveLimit}" Width="150" />
                <DataGridTextColumn Header="SoftNegativeLimit" Binding="{Binding SoftNegativeLimit}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- 뒤로가기 버튼 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="뒤로가기" Command="{Binding GoBackCommand}" Padding="20,6" />
        </StackPanel>
    </Grid>
</Page>
