<UserControl x:Class="EMC.ActualBondingForce"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:EMC">
    <Grid local:GridHelper.Rows="Auto, 1*">

        <TextBlock Text="{Binding Title}" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="4"
                   HorizontalAlignment="Center"/> 

        <lvc:CartesianChart Series="{Binding SeriesCollection}"
                            DisableAnimations="True"
                            LegendLocation="None" 
                            Grid.Row="1"
                            >
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Time (ms)"
                          MinValue="{Binding AxisMin}"
                          MaxValue="{Binding AxisMax}">
                    <lvc:Axis.Separator>
                        <lvc:Separator Step="{Binding TimeStep}"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>

            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Force (N)"
                          MinValue="{Binding MinForce}"
                          MaxValue="{Binding MaxForce}">
                    <lvc:Axis.Separator>
                        <lvc:Separator Step="{Binding ForceStep}"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</UserControl>
