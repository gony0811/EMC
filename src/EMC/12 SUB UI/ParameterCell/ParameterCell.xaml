<UserControl x:Class="EMC.ParameterCell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EMC"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/01 SYSTEM/Common/Styles/DefaultResource.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" SharedSizeGroup="NameCol"/>
            <ColumnDefinition Width="180" SharedSizeGroup="ValueCol"/>
        </Grid.ColumnDefinitions>

        <!-- 이름 -->
        <Border Style="{StaticResource HeaderCell}">
            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
        </Border>

        <!-- 값/설명 -->
        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1">
            <Grid>
                <!-- [1] ReadOnly=True → DisplayValue 표시 -->
                <TextBlock Text="{Binding DisplayValue}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           FontWeight="Normal">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ReadOnly}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <local:PlaceholderTextBox 
                        Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                        Placeholder="{Binding Description}"
                        Background="White"
                        Margin="0"
                        Padding="0">
                    <local:PlaceholderTextBox.Style>
                        <Style TargetType="local:PlaceholderTextBox">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ReadOnly}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </local:PlaceholderTextBox.Style>
                </local:PlaceholderTextBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
