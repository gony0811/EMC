<UserControl x:Class="EMC.AutoTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EMC"
             mc:Ignorable="d" >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/01 SYSTEM/Common/Styles/DefaultResource.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <GroupBox Header="Run Information" Style="{StaticResource GroupBox.CenterHeader}" Margin="4"></GroupBox>
            <GroupBox Header="Running Step" Grid.Row="1" Style="{StaticResource GroupBox.CenterHeader}" Margin="4"></GroupBox>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <GroupBox Header="Running Status" Style="{StaticResource GroupBox.CenterHeader}" Margin="4"></GroupBox>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="4" HorizontalAlignment="Center">
                <Button Content="INITIAL" Width="120" Height="80"></Button>
                <Button Content="START" Width="120" Height="80"></Button>
                <Button Content="STOP" Width="120" Height="80"></Button>
                <Button Content="RESET" Width="120" Height="80"></Button>
            </StackPanel>
            
        </Grid>
    </Grid>
</UserControl>
